# Logo source files

Put the designer's original working files here (`.ai`, `.eps`, `.psd`,
layered/editable files). Upload via **Add file → Upload files** on
github.com — see the README in `brand-assets/` for step-by-step help.
